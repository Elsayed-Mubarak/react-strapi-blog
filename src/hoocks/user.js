import React from 'react';
import Product from './product'
const User = (props) => {
    return (
        <div>
            <Product uInput={props.uInput} />

        </div>
    );

}

export default User;
